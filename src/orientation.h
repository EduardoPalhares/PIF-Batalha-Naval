#ifndef ORIENTATION_H
#define ORIENTATION_H

// Enum que define a orientação do navio no tabuleiro
enum Orientation {
    ORIENTATION_HORIZONTAL,   // navio deitado (linha)
    ORIENTATION_VERTICAL      // navio em pé (coluna)
};

#endif
